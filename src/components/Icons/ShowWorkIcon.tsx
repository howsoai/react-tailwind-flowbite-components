import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { TbMathFunction } from "react-icons/tb";

export const ShowWorkIcon: FC<IconBaseProps> = (props) => {
  return <TbMathFunction {...props} />;
};
