import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { SiDatabricks } from "react-icons/si";

export const DatabricksIcon: FC<IconBaseProps> = function (props) {
  return <SiDatabricks {...props} />;
};
