import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiLightningBolt } from "react-icons/hi";

export const RunProcessIcon: FC<IconBaseProps> = (props) => {
  return <HiLightningBolt {...props} />;
};
