import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { TbNetwork } from "react-icons/tb";

export const MetaLlamaIcon: FC<IconBaseProps> = (props) => (
  <TbNetwork {...props} />
);
