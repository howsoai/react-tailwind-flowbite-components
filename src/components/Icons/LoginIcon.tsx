import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiOutlineLogin } from "react-icons/hi";

export const LoginIcon: FC<IconBaseProps> = (props) => {
  return <HiOutlineLogin {...props} />;
};
