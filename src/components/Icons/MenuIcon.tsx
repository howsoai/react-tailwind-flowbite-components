import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiMenu } from "react-icons/hi";

export const MenuIcon: FC<IconBaseProps> = (props) => <HiMenu {...props} />;
