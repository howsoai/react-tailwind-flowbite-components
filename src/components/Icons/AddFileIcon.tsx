import { FC } from "react";
import { IconBaseProps } from "react-icons";
import { HiDocument } from "react-icons/hi";

export const AddFileIcon: FC<IconBaseProps> = (props) => {
  return <HiDocument {...props} />;
};
