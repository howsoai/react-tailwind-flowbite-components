import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiSparkles } from "react-icons/hi2";

export const PredictIcon: FC<IconBaseProps> = (props) => {
  return <HiSparkles {...props} />;
};
