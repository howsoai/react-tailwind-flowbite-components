import { FC } from "react";
import { IconBaseProps } from "react-icons";
import { HiDatabase } from "react-icons/hi";

export const DatabaseIcon: FC<IconBaseProps> = (props) => {
  return <HiDatabase {...props} />;
};
