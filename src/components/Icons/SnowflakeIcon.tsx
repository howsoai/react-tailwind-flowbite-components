import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { SiSnowflake } from "react-icons/si";

export const SnowflakeIcon: FC<IconBaseProps> = (props) => {
  return <SiSnowflake {...props} />;
};
