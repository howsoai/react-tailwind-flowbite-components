import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiAdjustments } from "react-icons/hi";

export const ConfigurationIcon: FC<IconBaseProps> = (props) => {
  return <HiAdjustments {...props} />;
};
