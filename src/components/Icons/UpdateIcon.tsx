import { FC } from "react";
import { IconBaseProps } from "react-icons";
import { HiRefresh } from "react-icons/hi";

export const UpdateIcon: FC<IconBaseProps> = (props) => {
  return <HiRefresh {...props} />;
};
