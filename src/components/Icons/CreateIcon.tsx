import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiPlus } from "react-icons/hi";

export const CreateIcon: FC<IconBaseProps> = (props) => {
  return <HiPlus {...props} />;
};
