import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiDownload } from "react-icons/hi";

export const DownloadIcon: FC<IconBaseProps> = (props) => {
  return <HiDownload {...props} />;
};
