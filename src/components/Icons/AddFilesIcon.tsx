import { FC } from "react";
import { IconBaseProps } from "react-icons";
import { HiDocuments } from "react-icons/hi";

export const AddFilesIcon: FC<IconBaseProps> = (props) => {
  return <HiDocuments {...props} />;
};
