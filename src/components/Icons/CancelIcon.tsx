import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiBan } from "react-icons/hi";

export const CancelIcon: FC<IconBaseProps> = (props) => <HiBan {...props} />;
