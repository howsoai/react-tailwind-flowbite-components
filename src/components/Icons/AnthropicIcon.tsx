import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { SiAnthropic } from "react-icons/si";

export const AnthropicIcon: FC<IconBaseProps> = (props) => {
  return <SiAnthropic {...props} />;
};
