import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { GiMeshNetwork } from "react-icons/gi";

export const LlmIcon: FC<IconBaseProps> = (props) => (
  <GiMeshNetwork {...props} />
);
