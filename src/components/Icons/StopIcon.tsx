import { type FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiStop } from "react-icons/hi";

export const StopIcon: FC<IconBaseProps> = (props) => <HiStop {...props} />;
