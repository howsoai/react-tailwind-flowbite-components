import { FC } from "react";
import { IconBaseProps } from "react-icons";
import { HiUpload } from "react-icons/hi";

export const UploadIcon: FC<IconBaseProps> = (props) => {
  return <HiUpload {...props} />;
};
